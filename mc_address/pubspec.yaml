name: mc_address
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  asp: ^1.3.0
  auto_injector: ^2.0.2
  uno: ^1.1.9
  flutter:
    sdk: flutter

dev_dependencies:
  mocktail: ^1.0.3
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
